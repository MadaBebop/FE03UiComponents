import React from 'react';
import { Spinner } from '@cgn/framework-ui-components/v1';
    
function Loading() {
  return (
    <Spinner large={large} center={center} />
  );
}

export default Loading;
 